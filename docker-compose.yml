# Use postgres/example user/password credentials
version: '3'

services:

  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: test123
    ports:
      - 5432:5432
